workflows:
  android-aar-workflow:
    name: Android Kotlin AAR Build
    max_build_duration: 60
    environment:
      vars: {}
    scripts:
      - name: Fix gradlew permissions
        script: chmod +x ./gradlew
      - name: Build AAR
        script: ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/aar/*.aar
